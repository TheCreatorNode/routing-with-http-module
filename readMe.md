Endpoints:

GET /students → list all students

GET /students?course=Science → filter by query

GET /students/:id → single student

POST /students → add a new student

PUT /students/:id → edit info

DELETE /students/:id → delete student

Students should have: { id, name, age, course }

Store in students.json file.

Add basic validation: name and course required.

Send readable error messages.

Bonus: Sort students alphabetically when listing.

# FLOW
